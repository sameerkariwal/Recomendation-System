#make sure you have anaconda3 installed and running this over jupyter lab/notebooks there, that will contain all the dependencies required
pip install jupyterlab
pip install numpy
pip install pandas
pip install scipy
pip install nltk
pip install plotly
pip install import_ipynb
pip install sklearn
conda install pytorch
pip install -U sentence-transformers